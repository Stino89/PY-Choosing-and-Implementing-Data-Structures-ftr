import numpy as np

# Define the array
a = np.array([1, 2, 3, 4])

# Define the function to multiply elements by 3
def multiply_elements_by_3(arr):
    return 3 * arr

# Call the function with array 'a'
result = multiply_elements_by_3(a)

# Print the result
print(result)
