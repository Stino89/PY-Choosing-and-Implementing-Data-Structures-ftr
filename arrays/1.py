import numpy as np

# Define the array
a = np.array([1, 2, 3, 4])

# Define the function to print elements of the array
def show_elements(arr):
    for el in arr:
        print(el)

# Call the function with array 'a'
show_elements(a)
